# 2026-09-13

## 面板 UI 视觉重构（精简大气）
- 文件：`boss.user.js`（原 `py.txt` 已改名）。样式以 `injectStyle()` 里的模板字符串注入（`style.textContent = \`...\``）。
- 做法：整块 CSS 重写，**保持所有类名与选择器不变**，只重构视觉，避免影响 JS 逻辑。
- 用 Node 脚本按 `style.textContent = \`` 与收尾 `` ` `` 定位切片替换（CSS 文内无反引号，`indexOf('\`;')` 安全）。
- 新设计要点：
  - 设计变量：新增 `--za-radius/sm/xs`、`--za-shadow/sm/lg`、`--za-ring`、`--za-bg-soft/sunken`、`--za-text-2`、`--za-border-strong`；主色保持青绿 `#0d9488`（`--za-primary`），危险色 `#dc2626`。
  - 入口按钮：渐变 + 圆角 + 阴影/位移 hover。
  - 顶栏/底栏：sticky + 半透明白 + `backdrop-filter` 毛玻璃；状态条改为整宽 sticky 条（top:60px），按 data-type 着色。
  - 区块：`h3` 前加 3px 主色竖标；`.za-section` 用 `border-top` 分隔，紧跟状态条的首个区块去边框。
  - 分段控件 `.za-segment` 改为胶囊式，内部 radio/checkbox 隐藏（`opacity:0` 保留可聚焦），`:has(input:checked)` 高亮。
  - 输入/按钮统一 34px 高、7px 圆角、聚焦环；chip 改圆角胶囊；`.za-radius-xs` 统一。
- 校验：`node --check` 通过。
- 交付预览：`panel-preview.html`（脚本自动抽取真实 CSS 生成，含收起按钮 + 展开面板示意）。

## JD 匹配「当前岗位得分」参考分
- 需求：在 JD 智能匹配板块「匹配阈值」右侧显示当前选中岗位的匹配分作为参考。
- 新增 `MatchPreview`（在 `JdMatchService` 之后）：`buildSignature()` 记录除阈值外的匹配配置指纹；`schedule(card)` 防抖 500ms 评估；`evaluate()` 拉详情后调 `JdMatchService.evaluate`；`report()` 供运行中直接写入结果；`syncWithConfig()` 处理启用/禁用与配置变更（仅阈值变化时复用旧分数，只刷新结论）。
- UI：`section[data-feature-section="jdMatch"]` 的阈值所在 `.za-grid-2` 内新增 `.za-match-ref` 徽标（标题/分数/结论/岗位名），refs 见 `runtime.ui.matchScoreRef*`，写入逻辑在 `UI.renderMatchRef(state)`（state: disabled/idle/loading/error/done）。
- 触发点：① `pageWindow` 捕获阶段 click 监听 `li.job-card-box`（运行中 `runtime.automationLoopActive` 为真时跳过，避免与主流程重复请求）；② `communicateWithCard` 判定后 `MatchPreview.report()`；③ `applyConfigToForm` 挂载时 `syncWithConfig()`；④ `change` 事件统一 `syncWithConfig()`，`input`（阈值）只刷新结论。
- 参考分结论阈值符号：`data-level="pass"/"fail"`，绿色 `#047857` / 琥珀 `#b54708`。
- `panel-preview.html` 已同步该徽标与 CSS。
- 校验：`node --check` 通过。

## 注意
- 面板 `.za-panel` 滚动容器为自身，header sticky top:0（min-height 60），status sticky top:60px，改动这两个高度需同步。
